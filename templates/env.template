# Claude Multi-Agent Coding System - Environment Variables
# Copy this file to ~/.env and fill in your actual values
# chmod 600 ~/.env (IMPORTANT: Keep this file private!)

# ============================================================================
# GitHub Authentication
# ============================================================================
# Create a Personal Access Token at: https://github.com/settings/tokens
# Required scopes: repo (all), workflow
GITHUB_TOKEN=ghp_YOUR_GITHUB_TOKEN_HERE

# ============================================================================
# Anthropic API
# ============================================================================
# Get your API key from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-YOUR_ANTHROPIC_KEY_HERE

# ============================================================================
# Docker Defaults
# ============================================================================
# Default memory limit for containers (e.g., 4g, 2g, 512m)
DEFAULT_DOCKER_MEMORY=4g

# Default CPU limit for containers (number of CPUs)
DEFAULT_DOCKER_CPUS=2

# ============================================================================
# Task Defaults
# ============================================================================
# Maximum cost per task in USD (prevents runaway API costs)
DEFAULT_MAX_COST=5.00

# Default base branch for PRs (usually 'main' or 'master')
DEFAULT_BASE_BRANCH=main

# ============================================================================
# System Settings
# ============================================================================
# Environment: production or development
NODE_ENV=production

# Log level: error, warn, info, debug
LOG_LEVEL=info
